#include<stdio.h>
void mergeSort(int arr[],int st,int end){
  int mid=st+(end-st)/2;
  mergeSort(odd)
}
void main(){
  int arr[]={2,10,5,17,15,4};
  int c=sizeof(arr)/sizeof(arr[0]);
   mergeSort(arr,0,c);
   for(int i=0; i<c; i++)
     printf("%d\t",arr[i]);
}